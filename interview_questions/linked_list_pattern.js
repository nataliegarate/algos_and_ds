function reOrderLinkedList(head) {
    let length = getLength(head);
    halfLength = Math.max(length);
    let list2Head = //get head for list 2
        let list1 = // returns head and sets last to null
            list2Head = reverseList(list2Head);
    mergeLists(list1, list2);
    return head;
}

function getLength(node) {
    //this gets length of LL
}

function merge(list1, list2) {
    //merges two lists
}

function makeListOne(node, halfLength) {
    // first List
}

function getListTwoHead(node, halfLength) {
    // return listTwo Head
}

function reverseList(node) {
    //reverse the list
}

function mergeLists(list1, list2) {
    //merges them
}